package ch.so.agi.hop.doclint.fixtures;

import org.apache.hop.core.annotations.Transform;

/** Fixture transform that only sets the mandatory annotation attributes. */
@Transform(id = "MINIMAL_TRANSFORM", name = "Minimal Transform")
public class MinimalTransformMeta {}
